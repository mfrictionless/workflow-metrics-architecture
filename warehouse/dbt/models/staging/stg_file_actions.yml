version: 2

models:
  - name: stg_file_actions
    description: One current row per file_action_id, deduplicated from raw_file_actions by _cdc_ts.
    columns:
      - name: file_action_id
        tests:
          - unique
          - not_null
